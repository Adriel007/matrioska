"""
Hierarchical memory system (§4.5 of the plan):

Working → Episodic → Semantic → Procedural
(curto)    (runs)     (grafo)    (padrões)

Each tier adds a layer of abstraction and retention time.
"""
